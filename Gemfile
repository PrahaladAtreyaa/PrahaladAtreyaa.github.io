# Gemfile

source "https://rubygems.org"

# GitHub Pages ships with Jekyll and many plugins by default.
# If you prefer using GitHub Pages directly, you can specify:
# gem "github-pages", group: :jekyll_plugins
# However, for local development and theme usage, we can do:

gem "github-pages", group: :jekyll_plugins
gem "minimal-mistakes-jekyll"
